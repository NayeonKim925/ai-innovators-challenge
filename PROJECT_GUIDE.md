# 프로젝트 개발 가이드

이 문서는 이 저장소가 다루는 데이터가 무엇인지, 왜 이런 순서로 개발이
진행되는지, 각 단계가 무슨 역할을 하는지, 그리고 현재 어디까지 진행됐는지를
정리한다.

---

## 1. 이 데이터셋이 무엇인가 (기초 개념)

**웨이퍼(wafer)**: 반도체 칩의 재료가 되는 얇은 원판. 이 원판 위에 여러
공정을 거쳐 미세한 회로 패턴을 새기면 그게 반도체 칩이 된다.

**에칭(etch, 식각)**: 웨이퍼 표면에서 필요 없는 부분을 깎아내고 원하는
패턴만 남기는 공정. 이 프로젝트가 다루는 데이터는 그 중에서도 **플라즈마
에칭**을 쓴다 — 기체를 전기적으로 이온화시켜 반응성이 강한 상태(플라즈마)로
만들고, 그 플라즈마가 웨이퍼 표면을 화학적·물리적으로 깎아낸다.

**왜 장비 이상이 문제가 되는가**: 에칭 장비는 가스 유량, 압력, RF(고주파)
전력 같은 값들을 정밀하게 유지해야 정확하게 깎인다. 이 값들이 미세하게
틀어지면 웨이퍼가 원하는 만큼 깎이지 않거나 과하게 깎여서 불량이 된다.
그래서 "장비 센서 이상 → 웨이퍼 불량"이라는 인과관계가 실제로 존재한다.

**이 데이터셋(Metal Etch Data)**: LAM 9600이라는 실제 플라즈마 에칭 장비로
웨이퍼 129장을 처리하면서 기록한 센서 데이터. 정상적으로 처리된 웨이퍼와,
일부러 특정 결함(가스 누출, 냉각 이상 등)을 유발한 웨이퍼가 섞여 있다.
출처: Eigenvector Research (Wise et al., 1999, *J. Chemometrics*).

**왜 파일이 3개로 나뉘어 있는가**: 같은 웨이퍼를 세 가지 서로 다른 종류의
센서로 각각 측정했기 때문이다.

| 파일 | 무엇을 측정하는가 | 쉽게 말하면 |
|---|---|---|
| MACHINE_Data.mat | 장비 자체가 기록하는 제어값 (가스 유량, 압력, RF 파워 등) | 장비의 "계기판" 기록 |
| OES_DATA.mat | 광방출분광 — 플라즈마가 내는 빛을 파장별로 분석 | 플라즈마가 "어떻게 반응하고 있는지"를 빛으로 읽은 것 |
| RFM_DATA.mat | RF 모니터 — 에칭에 쓰이는 고주파 전력 신호 자체 | 전력 신호의 "파형" 기록 |

정상 웨이퍼는 `calibration`, 결함 웨이퍼는 `test`라는 이름으로 각 파일에
들어있고, 결함 웨이퍼에는 `fault_names`라는 필드로 실제 결함 종류가 라벨로
붙어 있다.

---

## 2. 데이터 정합성이 왜 첫 단계의 취지인가

이 프로젝트가 만들려는 것은 "알람이 울리면 원인을 찾아 근거와 함께
제시하는 에이전트"다. 이게 성립하려면 대전제가 하나 있다 — **에이전트가
보는 센서 기록이 진짜 그 웨이퍼의 기록이 맞아야 한다.** 이 전제가
깨지면, 이후 아무리 정교한 원인분석 알고리즘을 돌려도 애초에 서로
무관한 데이터를 엮어서 나온 틀린 결론이 된다.

실제로 이 데이터를 열어보면서 확인된 문제가 두 가지 있었다.

1. **같은 웨이퍼를 파일마다 다른 이름으로 표기함**: 웨이퍼 3342번이
   MACHINE에서는 `l3342.txm`, OES에서는 `s3342.int`, RFM에서는
   `r3342.txt`로 저장돼 있다. 이름을 그대로 비교하면 접두문자와
   확장자가 달라 전혀 다른 웨이퍼처럼 보인다. 이름 중간의 숫자 4자리만
   진짜 공통 식별자다.
2. **파일마다 수록된 웨이퍼 개수가 다름**: MACHINE은 108(정상)+21(결함),
   OES와 RFM은 106+20이다. 일부 웨이퍼는 광학·RF 센서 기록이 아예 없다는
   뜻이라, 이걸 확인하지 않고 그냥 합치면 없는 데이터를 있는 것처럼
   취급하게 된다.

그래서 이 단계(데이터 파이프라이닝)의 역할은 단순히 "파일을 읽는 것"이
아니라, **어떤 웨이퍼가 실제로 어떤 센서군 데이터를 갖고 있는지를
명시적으로 확인하고 표시하는 것**이다. 이 작업의 결과로 웨이퍼 108개
중 104개는 세 센서군 데이터가 모두 존재하고(`complete_case`), 4개는
일부만 존재한다는 게 확인됐다. 이후 단계(이상탐지·원인후보 랭킹)는
이 확인된 정합성 위에서만 신뢰할 수 있는 결과를 낼 수 있다.

---

## 3. 전체 개발 프로세스와 각 단계의 역할

| 단계 | 과정 | 이 과정의 역할 | 산출물 | 상태 |
|---|---|---|---|---|
| 2-1 | 데이터 파이프라인 | 서로 다른 파일·명명규칙·커버리지를 가진 원본 데이터를 신뢰 가능한 공통 스키마로 정리 | `metadata.csv`, `variable_names.json`, `features/`, 로더 코드 | **완료** |
| 2-2 | 이상탐지 + 원인후보 엔진 | 정상 범위를 벗어난 신호를 잡아내고, 원인일 가능성이 높은 변수 후보를 순위로 산출 | 기존 NL→SQL 통계분석·causRCA 기반 causal discovery 자산을 이 스키마에 연결한 공통 모듈 | 예정 |
| 2-3 | RAG 문서 계층 | 원인후보에 대한 근거 문서(결함 설명, 온톨로지 구조화 자료)를 검색해 붙여줌 | 문서 인덱스 + 검색 도구 | 예정 |
| 2-4 | 에이전트 오케스트레이션 | 알람 발생 → 센서 조회 → 원인후보 산출 → 근거 문서 검색 → 보고서 생성까지 한 흐름으로 묶음 | tool-calling 기반 LLM 에이전트 | 예정 (2-2, 2-3 선행 필요) |
| 2-5 | 온톨로지 / 지식그래프 | "설비-공정단계-불량유형"을 노드로 하는 그래프를 만들어, 서로 다른 관점(설비 원인 vs 품질 결과)에서 같은 정보를 조회할 수 있게 함 | 그래프 스키마 + 적재 스크립트 | 예정 |
| 2-6 | 알람 UI | 사람이 실제로 알람을 받고 근거를 검토·승인하는 접점 | Slack 알람 + 대화형 조사 인터페이스 | 예정 |
| 3 | 평가·검증 | `fault_names` 같은 정답 라벨과 원인후보 랭킹을 대조해 precision@k 등으로 정량 측정 | 평가 스크립트 + 결과 리포트 | 예정 (전체 완료 후 마지막) |

---

## 4. 트랙별 분업

아래 트랙은 서로 다른 사람이 동시에 진행할 수 있도록 나눈 것이다.
화살표(→)는 선행 완료가 필요한 의존관계를 뜻한다.

- **트랙 A — 데이터 파이프라인 (2-1)**: 완료됨. `preprocess.py` 실행 결과인
  `metadata.csv`가 이후 모든 트랙의 입력 기준점이 된다.
- **트랙 B — 이상탐지·원인후보 엔진 (2-2)**: 트랙 A의 `metadata.csv` +
  `features/`를 입력으로 받아 원인후보 랭킹을 산출. 지금 바로 시작 가능.
- **트랙 C — RAG 문서 계층 (2-3)**: 트랙 A/B와 독립적으로 지금 바로 시작
  가능 (결함 유형별 참고문서 수집·인덱싱은 다른 작업과 병렬 진행 가능).
- **트랙 D — 에이전트 오케스트레이션 (2-4)**: 트랙 B, C의 결과물을
  도구(tool)로 감싸는 통합 작업 → **트랙 B, C가 어느 정도 진행된 뒤
  시작**하는 게 맞다.
- **트랙 E — 온톨로지/그래프 + 알람 UI (2-5, 2-6)**: 그래프 스키마 설계는
  지금 시작 가능하나, 실제 데이터 적재는 트랙 B의 원인후보 결과가 있어야
  의미가 생긴다.
- **트랙 F — 평가·검증 (3)**: 트랙 B가 원인후보를 낼 수 있어야 시작 가능한
  마지막 단계.

---

## 5. 현재까지 진행 상황 요약

- Metal Etch Data 3개 파일(MACHINE/OES/RFM)의 실제 내부 구조를 확인함 —
  공식 문서에 없던 wrapper 구조, 파일별로 다른 변수 필드명(`variables`
  vs `wave_axis`), 파일별로 다른 웨이퍼 명명 규칙을 전부 찾아냄
- 세 파일 간 진짜 공통 식별자(웨이퍼 번호 4자리)를 확정하고, 실제 겹침
  비율(108개 중 104개가 완전 일치)까지 확인함
- 이 구조를 반영한 로더 코드(`loaders/metal_etch_loader.py`)와 전처리
  스크립트(`preprocess.py`)를 작성해, `metadata.csv` 기준으로 누구나
  바로 다음 단계를 시작할 수 있는 상태로 만듦

즉 **2-1(데이터 파이프라인)까지 완료**됐고, 다음 착수 지점은 트랙 B
(이상탐지·원인후보 엔진)와 트랙 C(RAG 문서 계층)다.
